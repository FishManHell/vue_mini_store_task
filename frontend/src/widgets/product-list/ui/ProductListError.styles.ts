export const styles = {
  wrapper:
    'flex flex-col items-center justify-center gap-3 rounded-2xl border border-border bg-surface px-6 py-16 text-center',
  icon: 'text-error',
  title: 'text-lg font-semibold text-primary',
  description: 'max-w-sm text-sm text-secondary',
  retry:
    'mt-2 inline-flex items-center gap-1.5 rounded-xl border border-border bg-surface px-4 py-2 text-sm font-medium text-primary transition-colors hover:border-accent/30 hover:text-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent',
} as const
